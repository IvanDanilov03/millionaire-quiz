import questions from 'modules/Game/config/questions.json';
import { GameConfig } from 'modules/Game/types';

export const gameConfig: GameConfig = questions;

export const ANSWER_LETTERS = ['A', 'B', 'C', 'D', 'E', 'F'];
